{
  "name": "json-to-form-generator",
  "version": "1.0.0",
  "description": "A library for dynamically generating HTML forms from JSON schemas.",
  "main": "src2/index.js",
  "scripts": {
    "build": "rollup -c",
    "test": "jest",
    "dev": "rollup -c -w",
    "watch": "rollup -c -w"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "jest": "^29.0.0",
    "rollup": "^3.0.0"
  },
  "license": "MIT"
}
